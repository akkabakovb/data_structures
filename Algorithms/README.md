# Assignment 11 – Experimental Algorithm Analysis
Compare O(n²) vs O(n log n) uniqueness checks on huge random arrays.

| File | What it shows |
| ---- | ------------- |
| `uniqueMethods.java` | unique1 (nested loops) vs unique2 (sort + sweep) |
| `main` | times both, prints result & duration |

Run:  
javac uniqueMethods.java && java uniqueMethods
